# Corrected normal-jet frontier

Iter077L remains valid: local common collision N=SU(2)^4, real codimension12, transverse scaling degree20, maximum normal derivative order8. This does not make every formal derivative physically admissible.

The missing source lock is repaired by upstream Iter077Q re-review and Iter081R/S; exact paths/commits/blobs are in sources/CD003_SOURCE_CORRECTION_MANIFEST.json. The node compact gauge action removes arbitrary scalar tangential functions. At the identity orbit the normal fiber is V=spin1_SO(3) tensor Std5_S5, dimension12. Scalar invariants have grades0..8 `(1,0,1,0,3,0,7,0,16)` and total demonstrated dimension28. This is not full physical extension dimension.

A constant scalar supported term remains. Positive-order scalar invariant directions remain at2,4,6,8. Odd scalar invariant grades vanish in this counted sector. Boundary-covariant or representation-valued sectors remain outside an exhaustive classification.

CD003 supplies the regular-context identity

`<sum a_alpha partial_x^alpha delta_N,K> = sum (-1)^|alpha| <a_alpha,partial_x^alpha K|N>`.

It is valid for the stated admissible local terms and chart/density convention. It is not a physical gluing law or a canonical decomposition. On J_inv the actual source contexts would define a finite matrix of pairings; neither those physical contexts nor that matrix/rank are derived.

Flat-normal diagnostic kernels miss positive derivatives. The code's delta-prime and Phi(t)=t+a t^2 examples concern general distributional calculus, not physical invariant first-order modes or an admissible physical refinement. In particular d1=0 must not be contradicted by importing those controls as physics.

The next gate must determine source-derived coefficient equations and the physical insertion/transport domain, check mixing among allowed invariant jets, and justify the physical null quotient. Sufficiently many independent finite conditions are not ruled out on J_inv; choosing28 targets by convenience is not physical selection. No scalar redundancy, full jet elimination or regulator independence is established.

# Normal-Jet Frontier

## Pinned upstream authority

Controlling upstream result:

`pppuu7-cmd/Mechanism-Synthesis-QG-Reconstruction/results/ITER077L_SM_TRANSVERSE_SCALING_DEGREE_EXTENSION_THEOREM_RESULT.md`

- preregistration: `565a36453cf781b1af366da6960ab7f55e6605f1`;
- derivation: `1a6cb5331098287823e55c7db5452a976b8501a5`;
- classification: `ITER077L_SM_TRANSVERSE_SD20_CODIM12_EXTENSION_EXISTS_BUT_SCALING_ALONE_NONUNIQUE_ORDER8_LOCAL_FREEDOM_THEOREM_SCOPED`.

No normal-jet claim in this document is reconstructed from memory beyond that authority.

## What is established

In the frozen Iter077L scope:

```text
N = SU(2)^4 subset SL(2,C)^4
codim_R(N)=12
sd_N(t0)=20
20-12=8.
```

Standard finite-scaling-degree extension theory therefore supplies same-scaling-degree local extensions, but scaling degree alone does not make them unique. Differences may be supported on `N` and may involve normal derivatives through order `8`, with tangential coefficient data along `N` constrained by any additional conditions imposed separately.

The order-8 statement is a ceiling on the local normal derivative order in that theorem's scope. It is **not** a theorem that every formal normal derivative term through order 8 is independently source-compatible or physically admissible.

## What CD002-A does not touch

CD002-A studies an order-zero smooth tangential multiplier under a conditional normalized multiplicative law. It does not specify how composition acts on a generic supported term with normal derivatives.

A schematic local expression may be used only to name the future problem:

```text
sum_{|alpha|<=8} f_alpha(y) partial_n^alpha delta_N
```

but this line is **not** an asserted canonical decomposition, basis, independence theorem, or physical parameterization of `A_B`.

## Questions a future gate must answer

Before a selector can claim full local uniqueness, it must determine, on the actually source-derived domain:

- whether composition/refinement preserves normal derivative order or mixes orders;
- whether tangential coefficient functions for different normal jets mix under transport;
- whether gauge/orientation operations act diagonally or nontrivially on jet indices;
- whether a coherence law constrains each jet separately or couples the entire finite tower;
- whether some jet directions are source-null or boundary-null under a prospectively defined physical equivalence;
- whether the scalar normalization problem reappears independently at multiple jet orders;
- whether composition creates supported terms on additional collision strata.

All answers are currently `OPEN`.

## Selector burden

Fixing the order-zero tangential shape — even completely — is insufficient for a unique physical K5 extension unless one also proves one of the following for every remaining allowed sector:

1. it is fixed by the same source-derived law;
2. it is fixed by another independently authorized law;
3. it is absent under stronger established admissibility;
4. it is null in a defined physical equivalence quotient.

No one of these options is currently established for the normal-jet tower.

## Pro-run firewall

The next deep run may choose to analyze the normal-jet action early if that has higher information gain than a scalar/tangential gate. It must first pin the exact source composition/refinement object. This document does not preregister a jet transformation law and does not choose a representation of the jet tower.
